uint8 a = console_in;
uint8 count = 1;
if a >= 10 {
	while count <= a {
		console_out f"Iter num {count}\n";
		count += 1;
	}
	console_out "End of (za)loop";
}
else {
	console_out "Not if yopta";
}